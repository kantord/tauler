# The half of the example that is not tauler.
#
# AeroSpace reads gaps at config load time and exposes no command to change
# them, so this is where the rail's 140px is reserved. `outer.left` must match
# RAIL in ~/.config/tauler/layout.jsx.
start-at-login = true

[gaps]
    inner.horizontal = 8
    inner.vertical   = 8
    outer.top        = 8
    outer.bottom     = 8
    outer.right      = 8
    outer.left       = 140

[mode.main.binding]
    alt-1 = 'workspace 1'
    alt-2 = 'workspace 2'
    alt-3 = 'workspace 3'
    alt-shift-1 = 'move-node-to-workspace 1'
    alt-shift-2 = 'move-node-to-workspace 2'
    alt-shift-3 = 'move-node-to-workspace 3'
