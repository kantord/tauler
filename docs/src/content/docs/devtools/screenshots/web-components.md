---
title: devtools · web component screenshots
description: Browser screenshots of each component, captured against the built docs site.
---

**Source:** `tauler-web-e2e/tests/comparison.rs` (`every_mount_still_renders_its_component`)

**Regenerate:** `just web-e2e`

Not yet captured — this test needs Docker and a built docs site (`just web-e2e`), so no
screenshots are committed here yet. Once it has run, each component's browser render lands
at `docs/src/assets/devtools/web-components/{component}.png` and can be added below.
